
services:
  backend:
    image: sparsha-prod:latest
    working_dir: /app/backend
    command: ["node", "dist/server.js"]
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
    depends_on:
      - db

  frontend:
    image: sparsha-prod:latest
    working_dir: /app/frontend
    command: ["npx", "serve", "-s", "dist", "-l", "5173"]
    ports:
      - "5173:5173"
    environment:
      - NODE_ENV=production
    depends_on:
      - backend

  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: sparsha
      POSTGRES_PASSWORD: sparsha
      POSTGRES_DB: sparsha
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
